var first_name = "Ricky";
var last_name = "Bobby";
var age = 45;
var first = true;
var last = false;

if((age > 40) && (age < 50)) {
    // Then do something if the value of age is less than or equal to 40
} else if(age<0){
    // Maybe prevent something from happening
} else {
    // Do a different thing
}
